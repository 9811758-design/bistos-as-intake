# Bistos Service Validation Generator Design System

## 1. Atmosphere & Identity

신뢰할 수 있는 의료기기 업무도구다. 밝고 정돈된 작업면 위에서 발행 상태와 다음 행동이 즉시 구분되어야 한다. 시그니처는 비스토스 로고의 청색과 녹색을 함께 사용하는 얇은 브랜드 라인과 텍스트 중심 헤더다.

## 2. Color

| Role | Token | Value | Usage |
|---|---|---|---|
| Surface/primary | `SURFACE` | `#FFFFFF` | 카드, 입력 영역 |
| Surface/app | `BACKGROUND` | `#F3F7F9` | 창 배경 |
| Surface/subtle | `SURFACE_SUBTLE` | `#EAF2F5` | 선택·정보 배경 |
| Text/primary | `TEXT` | `#202428` | 제목, 본문 |
| Text/secondary | `MUTED` | `#66737C` | 안내, 메타데이터 |
| Border/default | `BORDER` | `#D7E2E7` | 카드·입력 경계 |
| Brand/blue | `BRAND_BLUE` | `#1D709B` | 주요 동작, 헤더 |
| Brand/blue-dark | `BRAND_BLUE_DARK` | `#11506F` | 헤더 깊이, hover |
| Brand/green | `BRAND_GREEN` | `#009B58` | 완료, 성공, 브랜드 라인 |
| Status/error | `ERROR` | `#B42318` | 입력 오류 |
| Status/warning | `WARNING` | `#B54708` | 주의 상태 |

색상은 위 토큰에서만 사용한다. 녹색은 완료·성공과 로고 포인트에만 사용하고, 기본 행동은 청색으로 유지한다.

## 3. Typography

| Level | Size | Weight | Usage |
|---|---:|---:|---|
| Display | 20px | 700 | 창 제목 |
| H2 | 12px | 700 | 카드 제목 |
| Body | 10px | 400 | 기본 UI |
| Body strong | 10px | 700 | 핵심 값, 버튼 |
| Caption | 9px | 400 | 안내, 상태 |

기본 글꼴은 `맑은 고딕`, 대체 글꼴은 Windows UI 기본 글꼴이다. 본문은 9px 미만으로 낮추지 않는다.

## 4. Spacing & Layout

기본 단위는 4px이다. 주요 간격은 4, 8, 12, 16, 20, 24, 32px만 사용한다. 메인 창 최소 크기는 940×620, 빠른 발행 창 최소 크기는 820×620이다. 헤더는 고정 높이의 브랜드 영역이고, 본문은 카드 스택과 확장 가능한 표로 구성한다.

## 5. Components

### Brand Header
- 구조: 청색 배경, 좌측 제목·설명, 하단 녹색 강조선. 공식 로고 자산이 제공된 화면에서만 우측 로고를 추가한다.
- 상태: 정적이며 본문 스크롤과 분리
- 접근성: 로고가 없어도 제목 텍스트로 화면 목적을 알 수 있음

### Card
- 구조: 흰 배경, 얇은 경계, 16px 내부 여백
- 변형: 설정 카드, 목록 카드, 입력 카드, 미리보기 카드
- 상태: 기본, 오류 안내, 성공 안내

### Action Button
- 변형: Primary(청색), Success(녹색), Secondary(흰색)
- 상태: default, hover, pressed, focus, disabled
- 접근성: 텍스트만으로 행동을 설명하며 키보드 포커스를 유지

### Data Table
- 구조: 고정 헤더, 30px 행, 세로·가로 스크롤
- 상태: 미발행, 발행완료, 미입력, 입력 완료, 선택
- 접근성: 색상 외에 체크문자와 발행상태 텍스트를 함께 표시

### Receivables Task Toolbar
- 구조: 좌측에 Outlook 새로고침과 10분 자동 갱신 안내, 우측에 전체 목록 Excel 저장
- 상태: 기본, 새로고침 중, 오류, 비활성
- 접근성: 자동 갱신 여부와 수동 실행 행동을 텍스트로 명시

### Task Status Filter
- 구조: 전체, 미입력, 입력 완료 라디오 선택과 상태별 건수 카드
- 상태: 기본, 선택, 키보드 포커스
- 접근성: 색상 대신 상태명과 건수를 함께 표시

### Task Status Actions
- 구조: 선택 행을 입력 완료로 바꾸거나 미입력으로 되돌리는 두 개의 명시적 버튼
- 상태: 선택 없음 비활성, 현재 상태에 맞는 동작만 활성
- 접근성: 행의 현재 상태가 표 텍스트로도 항상 표시됨

### Status Bar
- 구조: 화면 하단 한 줄 안내
- 상태: 정보(청색), 성공(녹색), 오류(적색)

### A/S Search Pane
- 구조: 연도·검색어·종료상태 필터와 검색 결과 표를 한 카드 안에 배치
- 상태: 빈 목록, 검색 중, 결과 있음, 오류, 선택
- 접근성: 서비스번호·의뢰자·병원·모델·증상·종료상태를 텍스트 열로 표시

### Sectioned Record Form
- 구조: 접수정보, 처리정보, 비용·비고 탭과 4px 배수의 2열 입력 그리드
- 상태: 신규 입력, 기존 행 편집, 저장 중, 입력 오류, 저장 완료
- 접근성: 모든 입력에 고정 라벨을 제공하고 Tab 순서가 시각적 순서를 따름

### Save Mode Banner
- 구조: 서비스번호와 현재 저장 모드를 한 줄에 표시
- 상태: 신규 접수(청색), 기존 행 덮어쓰기(주의색)
- 접근성: 색상뿐 아니라 `신규 접수` 또는 `기존 행 덮어쓰기` 문구를 항상 표시

### Similar Case Recommendation Dialog
- 구조: 상단에 분석 건수 안내, 중앙에 유사도·접수번호·모델·증상 목록, 하단에 선택 사례의 증상·불량원인·대응조치 상세
- 상태: 추천 결과 있음, 결과 없음, 선택, 원인·조치 적용 완료
- 동작: `해결 추천 Ctrl+R`로 열고 `대응조치만 적용` 또는 `원인·조치 적용`을 선택
- 학습: 사용자가 적용한 해결 조합은 로컬 해시 키와 적용 횟수만 저장하며 다음 추천 순위에 가중치로 반영
- 접근성: 유사도를 숫자 백분율로 표시하고, 적용 범위를 버튼 문구로 명시하며, 기존 사용자가 입력한 증상은 덮어쓰지 않음

### Overdue Alert Banner
- 구조: 대시보드 상단의 얇은 배너이며 4px 단위 여백으로 경고 제목, 지연 건수, 즉시 이동 동작을 한 줄에 배치한다. 색상 토큰은 `WARNING`, `SURFACE_SUBTLE`, `BRAND_BLUE`만 사용한다.
- 상태: default는 지연 건수와 이동 동작을 표시, loading은 건수 자리에 `확인 중` 텍스트 표시, empty는 배너를 숨기지 않고 `지연 없음`을 표시, error는 지연 집계 실패 문구와 재시도 동작 표시, selected는 관련 필터가 적용되었음을 텍스트로 표시
- 접근성: 경고는 색상만으로 전달하지 않고 `지연` 문구, 건수, 보조 아이콘 또는 텍스트 상태를 함께 제공한다. 시작 시 업무를 막는 모달로 띄우지 않는다.
- 키보드: Tab으로 배너 동작에 진입하고 Enter 또는 Space로 지연 목록으로 이동한다. Esc는 포커스를 해제하지 않고 현재 대시보드 맥락을 유지한다.

### Analytics Date Filter
- 구조: 4px 단위 간격의 시작일, 종료일, 빠른 기간 선택, 적용 버튼으로 구성한다. 선택 배경과 강조선은 `SURFACE_SUBTLE`, 실행 동작은 `BRAND_BLUE`, 잘못된 기간 안내는 `WARNING`만 사용한다.
- 상태: default는 현재 기간을 표시, loading은 적용 버튼을 비활성화하고 `불러오는 중` 표시, empty는 선택 가능한 데이터 기간이 없음을 표시, error는 날짜 범위 오류와 수정 지점을 문구로 표시, selected는 적용된 기간 라벨을 유지 표시
- 접근성: 각 입력은 고정 라벨을 가지며 오류는 색상뿐 아니라 설명 문구로 연결한다. 날짜 형식은 화면에 보이는 텍스트로 안내한다.
- 키보드: Tab 순서는 시작일, 종료일, 빠른 기간, 적용 순서를 따른다. Enter는 현재 필터를 적용하고 방향키는 빠른 기간 선택 안에서만 이동한다.

### Analytics Summary
- 구조: 4px 단위 카드 행으로 총 접수, 처리 중, 지연, 완료 같은 핵심 지표를 표시한다. 선택·보조 배경은 `SURFACE_SUBTLE`, 주요 숫자 강조는 `BRAND_BLUE`, 주의 지표는 `WARNING`만 사용한다.
- 상태: default는 지표명·숫자·기간을 표시, loading은 숫자 자리에 `집계 중` 표시, empty는 `데이터 없음`과 기간을 표시, error는 집계 실패와 재시도 가능 여부를 표시, selected는 현재 표 필터와 연결된 지표임을 텍스트로 표시
- 접근성: 지표는 숫자와 라벨을 함께 읽을 수 있어야 하며 지연 지표는 색상만으로 구분하지 않는다. 단위와 기간을 생략하지 않는다.
- 키보드: 요약 항목이 필터 역할을 할 때만 Tab 정지점이 된다. Enter 또는 Space로 해당 지표 필터를 선택하고, 선택 상태는 표 제목에도 반영한다.

### Analytics Tabular View
- 구조: 4px 단위의 표 제목, 필터 결과 설명, 고정 헤더, 행 목록, 하단 상태 문구로 구성한다. 선택 행 배경은 `SURFACE_SUBTLE`, 정렬·주요 링크는 `BRAND_BLUE`, 주의 행 표식은 `WARNING`만 사용한다.
- 상태: default는 행과 집계 기준을 표시, loading은 기존 헤더를 유지하고 본문에 `불러오는 중` 표시, empty는 필터 조건과 함께 결과 없음 표시, error는 표 영역 안에 오류와 재시도 동작 표시, selected는 행 선택과 상세 패널 연결 상태를 텍스트로 표시
- 접근성: 헤더와 셀의 관계가 분명해야 하며 주의 행은 색상뿐 아니라 `지연` 또는 `확인 필요` 텍스트를 포함한다. 시작 시 표 확인을 막는 모달을 사용하지 않는다.
- 키보드: Tab은 표 도구와 선택 행 진입에 사용하고, 방향키는 행 이동에 사용한다. Enter는 행 선택, Space는 현재 행 토글, Home과 End는 현재 표의 첫 행과 마지막 행으로 이동한다.
- 집계 기준: `모델별 A/S 발생건수` 표는 시트의 `처리완료월` 날짜(V열)를 선택 기간과 비교하고, 완료 날짜가 없는 행은 제외한다.

## 6. Motion & Interaction

데스크톱 업무도구이므로 장식적 애니메이션은 사용하지 않는다. 버튼 pressed 상태와 창 포커스만 플랫폼 기본 상호작용을 따른다. 행은 Ctrl 없이 클릭으로 선택하며, 빠른 발행은 Ctrl+Enter를 지원한다.

## 7. Depth & Surface

전략은 `mixed`다. 카드에는 1px 경계와 미세한 tonal shift를 사용한다. 팝업·대화상자는 운영체제 기본 그림자를 사용하며, 추가적인 강한 그림자나 그라데이션은 사용하지 않는다.

## 8. Android Material 3 Extension

Windows 도구의 신뢰감은 유지하되 Android에서는 Material 3 컴포넌트와 큰 터치 목표를 사용한다. 앱은 한국어 전용이며 첫 화면은 설명형 랜딩이 아니라 바로 업무를 시작하는 홈 화면이다.

### Mobile Tokens

| Role | Token | Value | Flutter usage |
|---|---|---:|---|
| Primary | `primary` | `BRAND_BLUE` | 주요 버튼, 선택 탭, AppBar |
| Primary container | `primaryContainer` | `#D7EDF6` | 선택된 탐색 항목, 정보 배너 |
| Secondary | `secondary` | `BRAND_GREEN` | 완료 상태, 저장 성공 |
| Warning container | `warningContainer` | `#FFF3D6` | 미처리 경고, 기간 오류 |
| Error container | `errorContainer` | `#FDECEC` | 저장 실패, 인증 실패 |
| Closed row | `closedRow` | `#E5E7EB` | 종료 처리 행 배경 |
| Service number | `serviceNumberCell` | `#FFF2CC` | 일반 DS 서비스번호 셀 배경 |

색상은 기존 토큰과 위 확장 토큰만 사용한다. 그라데이션, 장식용 그림자, 과한 단색 테마는 사용하지 않는다.

### Korean Typography

Flutter 기본 글꼴은 Android 시스템 한국어 글꼴을 사용하고, 모든 텍스트는 `TextScaler` 200%에서도 잘리지 않아야 한다. 입력 라벨은 13sp 이상, 본문은 14sp 이상, 버튼은 15sp 이상을 기본으로 한다. 글자 간격은 0이며 화면 너비에 따라 글꼴 크기를 비례 확대하지 않는다.

### Touch And Layout

모든 누를 수 있는 요소는 최소 48dp 높이와 48dp 너비를 가진다. 입력 필드는 한 줄 높이 52dp 이상, 주요 저장 버튼은 56dp 이상이다. 화면 폭 600dp 미만은 1열 폼, 600~839dp는 2열 폼, 840dp 이상은 좌측 NavigationRail과 우측 2-pane 레이아웃을 사용한다.

### Navigation

휴대폰 하단 탐색은 `접수`, `검색`, `미처리`, `통계` 네 항목으로 고정한다. 태블릿 840dp 이상에서는 NavigationRail로 바꾸고, 검색·미처리·통계에서 목록과 상세를 동시에 볼 수 있는 2-pane을 허용한다. 홈 화면에는 신규 A/S 접수, 접수 내역 검색, 미처리 내역, 통계 대시보드, 최근 접수 내역, Google Sheets 연결 상태가 한 화면에서 스캔 가능해야 한다.

### Mobile Form Sections

신규 접수와 상세 수정은 다섯 섹션으로 나눈다.

- 접수 기본: 접수일, 접수자, 지역, 의뢰자, 병원명, 연락처
- 제품 정보: 제품명/모델, 시리얼번호, 생산년월, 보증기간, 초기불량, 의뢰물품 및 수량, 입고 여부/입고일
- 증상/분류: 불량 대분류, 증상 및 요청사항, 불만 여부
- 처리 내용: 처리자, 불량원인, 대응조치, 처리결과, 처리완료일, 처리완료월, 서비스 후 검증
- 정산/기타: ERP 관련 날짜, 청구금액, 입금액, 비고, 운송장, 운송비, 기타비용, 종료상태

서비스 접수번호, 접수월, AF 빈 열은 사용자가 직접 편집하지 않는다. 처리자는 항상 `장진영`으로 보이고 잠금 상태이며 저장 시에도 `장진영`으로 강제된다.

### State Rules

상태는 화면을 막지 않는 배너와 필드 근처 오류로 표시한다. 필수 상태는 `loading`, `empty`, `offline`, `authRequired`, `authExpired`, `saveFailed`, `conflict`, `retrying`, `draftRestored`, `saved`, `closed`다. 네트워크 오류나 인증 오류가 있어도 작성 중 입력은 사라지지 않아야 하며, 재시도 버튼은 실패한 동작 옆에 둔다.

### Sheets Formatting

일반 DS 서비스번호 셀은 노란색 계열 `serviceNumberCell` 배경을 유지한다. BCM 제품군은 서비스번호가 날짜 형식이므로 DS 순번 색상 규칙을 적용하지 않는다. 종료상태가 `종료`인 행은 행 전체에 회색 계열 `closedRow`를 적용하되, 서비스번호 셀의 고유 배경색은 유지한다. 앱 내부 리스트도 같은 의미 체계를 따라 종료 행은 회색 배경과 `종료` 텍스트를 함께 표시한다.

### Charts And Dashboard

대시보드는 사용자가 시작일과 종료일을 직접 지정한다. 모델별 A/S 발생 건수, 월별 불량원인, 반복 고장, 미처리, 유상/무상은 숫자 요약 카드와 막대/원형 그래프로 함께 제공한다. 모델별 A/S 발생 건수는 `처리완료월` 또는 처리완료일 기준 날짜가 선택 기간 안에 있는 행만 집계하며, 완료 날짜가 없는 행은 별도 미처리 지표로 보낸다. 그래프는 색상만으로 의미를 전달하지 않고 범례와 숫자를 함께 표시한다.

### Privacy And Security UX

Google Sheets 비밀키나 서비스 계정 JSON은 APK 안에 넣지 않는다. 연결 상태는 `연결됨`, `로그인 필요`, `오프라인`, `서버 오류`로 짧게 표시한다. 개인정보 화면 캡처 제한 옵션을 설정에 제공하고, 전화번호·의뢰자·병원명은 추천 학습 특징값과 로그에서 제외한다. 로컬 임시 저장은 암호화 저장소를 사용한다.

### Accessibility

모든 아이콘 버튼은 tooltip 또는 semantic label을 가진다. 날짜 선택은 달력, 전화번호는 숫자 키패드, 금액은 천 단위 구분 입력을 사용한다. 저장 전 최종 확인 화면은 신규 저장과 기존 행 덮어쓰기를 문구로 명확히 구분한다. 오류가 생기면 해당 필드로 포커스를 이동하고, 색상 외에 오류 텍스트를 제공한다.
